public class celular {
   //Atributos 
   string marca;
   string modelo;
   int bateria = 100;
    //Metodos
    * Firma del metodo
    * público
    * void --> no retorna
    * el método tiene como nombre "llamar"
    * tiene un parémetro y es de tipo entero
    * /
    public void llamar( int numero){
          system.out.printIn("Llamamdo al "+ numero+ " ..."):
         this.bateria-= 5;
        }
}